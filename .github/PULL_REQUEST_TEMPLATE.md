## What & why

## Checklist
- [ ] `make scanner test` passes (`-Werror` clean)
- [ ] Docs updated (`docs/`, ABI/detector specs if changed)
- [ ] Kmod changes: built on 6.8 and `tools/vm-test.sh` passes with clean `dmesg`
- [ ] Detection only: no offensive/evasion code outside `testrk/`
